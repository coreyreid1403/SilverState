import { CreatorSettings } from "./CreatorSetings";

/**
 * User settings
 */
 export class UserSettings{

    /**
     * settings for creating packs
     * what packs they own, ect
     */
    creatorSettings: CreatorSettings = new CreatorSettings();
 
}