export class WineYear {
  year: number;
  count: number;
  cost: number;

  constructor(
    year: number,
    count: number,
    cost: number,
  ) {
    this.year = year;
    this.count = count;
    this.cost = cost;
  }
}
